server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Neighlink/config-data.git
          username: ${{secrets.USERNAME}}
          password: ${{secrets.PASSWORD}}
          default-label: "main"

  security:
    user:
      name: root
      password: s3cr3t
