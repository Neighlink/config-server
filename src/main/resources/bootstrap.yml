server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Neighlink/config-data.git
          username: mugarte-cba
          password: Abc07110511ab
          default-label: "dev/miguel"

  security:
    user:
      name: root
      password: s3cr3t
